"""Adapters layer - implements ports to connect with external systems.

Inbound adapters: Handle incoming requests (e.g., REST API, CLI, GraphQL)
Outbound adapters: Handle outgoing operations (e.g., database, external APIs)
"""

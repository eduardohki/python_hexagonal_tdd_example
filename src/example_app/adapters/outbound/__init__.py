"""Outbound adapters (driven adapters).

This package contains implementations of output ports defined in the domain layer.
These adapters interact with external systems like databases, APIs, file systems, etc.

Examples:
    - Repository implementations (PostgreSQL, MongoDB, in-memory)
    - External API clients
    - Message queue publishers
    - Email/SMS services
"""

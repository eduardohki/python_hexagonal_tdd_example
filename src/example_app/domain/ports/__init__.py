"""
Domain Ports - Interfaces that define the contracts between the domain and the outside world.

This package contains:
- Inbound ports (driven ports): Interfaces implemented by use cases, called by adapters
- Outbound ports (driving ports): Interfaces that the domain depends on, implemented by adapters
"""
